{
  "version": 1,
  "generatedAt": "2026-07-24T12:18:31.353Z",
  "mcp": [
    {
      "kind": "mcp",
      "id": "filesystem",
      "name": "filesystem",
      "description": "Replace the path. In GrokCode Settings → MCP, paste the command line as-is.",
      "transport": "stdio",
      "command": "npx -y @modelcontextprotocol/server-filesystem /path/to/allowed/dir",
      "url": "",
      "path": "examples/mcp/filesystem.stdio.example.json",
      "template": {
        "name": "filesystem",
        "transport": "stdio",
        "command": "npx -y @modelcontextprotocol/server-filesystem /path/to/allowed/dir",
        "enabled": true,
        "note": "Replace the path. In GrokCode Settings → MCP, paste the command line as-is."
      }
    },
    {
      "kind": "mcp",
      "id": "my-remote",
      "name": "my-remote",
      "description": "Prefer env-based secrets. For slow remotes set startup_timeout_sec = 120 in config.toml.",
      "transport": "http",
      "command": "",
      "url": "https://mcp.example.com/mcp",
      "path": "examples/mcp/http.remote.example.json",
      "template": {
        "name": "my-remote",
        "transport": "http",
        "url": "https://mcp.example.com/mcp",
        "header": "Authorization: Bearer YOUR_TOKEN",
        "enabled": true,
        "note": "Prefer env-based secrets. For slow remotes set startup_timeout_sec = 120 in config.toml."
      }
    }
  ],
  "skills": [
    {
      "kind": "skill",
      "id": "sample-commit",
      "name": "sample-commit",
      "description": "Create a clear conventional commit from staged changes. Use when the user asks to commit or /commit.",
      "path": "examples/skills/sample-commit/SKILL.md",
      "bodyPreview": "\n# Sample commit skill\n\n1. Run `git status` and `git diff --staged`\n2. Summarize intent in one sentence\n3. Propose a conventional commit message (`feat:`, `fix:`, `docs:`, …)\n4. Only commit after the user confirms (unless they said to commit immediately)\n"
    },
    {
      "kind": "skill",
      "id": "skill-pack-template",
      "name": "skill-pack-template",
      "description": ">",
      "path": "examples/skills/skill-pack-template/SKILL.md",
      "bodyPreview": "\n# Skill pack template\n\nProgressive loading (inspired by common agent skill layouts, GrokCode-native):\n\n1. **Metadata** — name + description (always available for matching)\n2. **This body** — short procedure when skill triggers\n3. **references/** — load only when needed (schemas, long docs)\n4. **scripts/** — deterministic helpers the agent can run\n5. **assets/** — templates/images for output, not "
    }
  ],
  "plugins": [
    {
      "kind": "plugin-source",
      "id": "xai-official",
      "name": "xAI Official Marketplace",
      "description": "官方插件市场源（需 grok plugin marketplace add）",
      "source": "https://github.com/xai-org/plugin-marketplace.git"
    }
  ]
}