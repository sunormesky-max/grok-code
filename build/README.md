# Build resources

Place optional icons here for electron-builder:

| File | Use |
|------|-----|
| `icon.ico` | Windows |
| `icon.png` | Linux (512×512+) |
| `icon.icns` | macOS |

Without custom icons, electron-builder uses defaults.
